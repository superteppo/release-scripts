#!/usr/bin/env bash
#MISE description="Generate a changelog entry from Git history"
#MISE dir="{{cwd}}"
set -euo pipefail
REPOSITORY_ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)
exec "$REPOSITORY_ROOT/lib/changelog.sh" "$@"
