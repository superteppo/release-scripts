#!/usr/bin/env bash
#MISE description="Retry publishing a local tag as a GitHub Release"
#MISE dir="{{cwd}}"
set -euo pipefail
REPOSITORY_ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)
exec "$REPOSITORY_ROOT/lib/github.sh" "$@"
