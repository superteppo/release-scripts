#!/usr/bin/env bash
#MISE description="Upload release artifacts to Dropbox"
#MISE dir="{{cwd}}"
set -euo pipefail
REPOSITORY_ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)
exec "$REPOSITORY_ROOT/lib/dropbox.sh" "$@"
