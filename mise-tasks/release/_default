#!/usr/bin/env bash
#MISE description="Create a numbered, tagged GitHub release"
#MISE dir="{{cwd}}"
set -euo pipefail
REPOSITORY_ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)
exec "$REPOSITORY_ROOT/lib/release.sh" "$@"
